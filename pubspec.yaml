name: ordered_iterable
description: Ordered iterable is a library for multi-level (hierarchical) sorting of collections (orderBy, orderByDescending, thenBy, thenByDescending).
version: 1.0.7
homepage: https://github.com/mezoni/ordered_iterable
topics:
  - sort
  - sorting
  - collection
environment:
  sdk: '>=3.0.0 <4.0.0'
dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.8
